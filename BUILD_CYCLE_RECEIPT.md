# BUILD_CYCLE_RECEIPT - Groove Serpent 1.1.0

Status: LOCAL_RELEASE_BOUNDARY - exact approval is recorded externally.

Date: 2026-09-11

## Authority and acceptance

The source is the local `codex/1.1.0-dev` branch, based on published `v1.0.0`
commit `a3923fdf337a1a2284ec4defd03355032706883c`. The owner designated Astra
to review, repair, and issue the final local decision. No push, tag, release,
or public deployment is authorized.

The completion contract is `BUILD_CONTRACT_1.1.md`. Required evidence includes
the exact candidate test and browser results, quality gates, reproducible source
and Python distributions, clean installation, privacy audit, target-filesystem
acceptance, a real-album preservation and replay check, independent blind review,
and a verified Epic Continuum checkpoint.

## Current release boundary

The preceding development cycle repaired public-source privacy defects,
partial audition-window credit, nonportable legacy relative paths, cleanup
ownership, and source-stream precision/channel authority. It received local
approval on its own frozen bytes. This stable-version preparation changes
metadata, documentation, artwork, final release provenance, and refusal of
stale or decoder-error media renders. All applicable
gates and independent review must be repeated. Historical green logs do not
establish a pass for changed source.

The owner's historical rejection of one auditioned repair remains authoritative
only for its exact preview bytes. The fresh real-album workflow must preserve
that decision, leave all other candidates undecided, and use restoration mode
`none`. Private recording names, project details, and audio hashes belong in
local evidence only.

## Evidence and final decision

This tracked document defines the evidence boundary; it is not a claim that
unwritten receipts already exist. A separate immutable local completion receipt
must bind the receipt-inclusive commit/tree, every delivered artifact and gate,
reviewer verdict, and Continuum operation proof. That external receipt avoids
the circular dependency of embedding a commit's hash inside itself.

Only a complete external receipt can classify these bytes as `PROMOTABLE` and
record Astra's `APPROVE`. Without that receipt the decision is `HOLD_REVIEW`.
The handoff includes source and Python distributions, not a newly certified
Windows portable. Hosted CI requires a future authorized GitHub upload; local
evidence cannot be presented as that unrun hosted proof.

## Preservation, rollback, and scope

Original captures and previous reviewed projects, artifacts, and evidence are
preserved. New work uses separate destinations with no-overwrite publication.
Rollback is the preceding local commit or published baseline after preserving
evidence. Existing public history is preserved; future public artifacts contain
the sanitized source tree, not a complete-history bundle.

Native Windows tests and the actual destination filesystem require fresh proof.
Type checks and modeled path tests alone do not establish native Linux/macOS
runtime or interactive listening acceptance. One-directory rename establishes
application-failure atomicity, not power-loss durability. Owner listening remains
the authority for audible restoration quality.
